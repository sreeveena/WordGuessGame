//array of strings in a variable computerGuess
//computer picks a rondom word from the array of computerGuess
//use MathRandom() to pick a random word from the array using the number generated by Math function
//assign the random word to a variable called currentWord
//create a virable called numOfGuesses and assign it 12
//make a for loop to iterate currentWord.length times to generate a string with  -
//create a variable for - count as dashCount
//create a variable to count wins
//create a variable to count losses
// User enters key and need to capture in an variable presentLetter

    //concatenate the userGuess with the presentLetter
    //check if presentLetter is present in the currentWord in a for loop and 
        //reveal(replace the - with letter) if present in the exact locations
            //decrement dashCount
        //decrement the numOfGuesses 
        //else just decrement the numOfGuesses

    //check if the numOfGuesses is equal to zero 
        //reset the values of numOfGuesses to 12
        //computer picks a rondom word using Math library
        //make a for loop to iterate currentWord.length times to generate a string with  -
        //set dashCount to the new word length generated
        //increment the losses
    //check if dashCount is equal to Zero then
        //increment the wins
        //reset the values of numOfGuesses to 12
        //computer picks a rondom word using Math library
        //make a for loop to iterate currentWord.length times to generate a string with  -
        //set dashCount to the new word length generated
        //change the picture using 
